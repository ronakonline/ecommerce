{{> csslinks}}

{{> header}}




    <!--section start-->
    <section class="register-page section-b-space">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <center>

                    <h3>Verify Email</h3>
                    <p>Verification email is sent on {{session.email}}</p>
                    {{#if messages}}
                        <p>{{messages}}</p>
                    {{/if}}
                    <p><a href="/resendemail?email={{session.email}}">Resend Email?</a></p>
                    </center>
                </div>
            </div>
        </div>
    </section>
    <!--Section ends-->


{{> footer}}
{{> jslinks}}